version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "5001:5001"

  frontend:
    build: ./frontend
    ports:
      - "5000:5000"
    environment:
      - BACKEND_URL=http://backend:5001/add
    depends_on:
      - backend